@model IEnumerable<LibraryWebApplication1.Team>

@{
    ViewData["Title"] = "Запит 3";
}

<h1>Запит 3</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TManager)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TManager)
            </td>
        </tr>
}
    </tbody>
</table>
